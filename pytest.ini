[pytest]
DJANGO_SETTINGS_MODULE = TicketRecommend.settings
python_files = test_*.py
testpaths = tests
addopts = -ra -m "not e2e"
markers =
    e2e: browser-level Playwright tests (run explicitly with -m e2e)
